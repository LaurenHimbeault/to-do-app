{
  "name": "todo-backend",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "prisma:generate": "prisma generate",
    "prisma:deploy": "prisma migrate deploy",
    "seed": "tsx prisma/seed.ts",
    "migrate:direct": "env-cmd -f .env.migrate prisma migrate deploy",
    "seed:direct": "env-cmd -f .env.migrate tsx prisma/seed.ts"
  },
  "dependencies": {
    "@fastify/cors": "^9.0.1",
    "@prisma/client": "^5.18.0",
    "fastify": "^4.28.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^24.5.0",
    "env-cmd": "^11.0.0",
    "prisma": "^5.18.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.2"
  }
}
